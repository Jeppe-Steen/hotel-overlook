import Style from './HeroSlider.module.scss';

const HeroSlider = () => {
    return (
        <section className={Style.heroSlider}>
            <p>HeroSlider</p>
        </section>
    )
}

export default HeroSlider;